#include<bits/stdc++.h>
using namespace std;
#define N 301234
#define Z 26
int n,len[N],fail[N],s[N],ch[N][Z];
char str[N];
int trans(int x,int p){
	for(int i=p;s[i-len[i]-1]!=s[p];)
}
int insert(int x){
	for()
}
int main(){
	int i;
	for(scanf("%s",str+1),n=strlen(str+1),i=1;i<=n;i++)
		insert(str[i]-'a');
}
 
